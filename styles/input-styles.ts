import {StyleSheet} from "react-native";

const input = StyleSheet.create({
    default: {
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
    }
});

export default input;
